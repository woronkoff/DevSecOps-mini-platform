version: "3.9"

services:
  app:
    build: .
    container_name: devsecops_app

    ports:
      - "5000:5000"
      
    environment:
      - FLASK_ENV=development

    volumes:
      - .:/app

    restart: unless-stopped